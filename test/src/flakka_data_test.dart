// ignore_for_file: prefer_const_constructors
import 'package:flakka_data/flakka_data.dart';
import 'package:test/test.dart';

void main() {
  group('FlakkaData', () {
    test('can be instantiated', () {
      expect(FlakkaData(), isNotNull);
    });
  });
}
